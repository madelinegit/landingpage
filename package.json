{
  "name": "routesnapper",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
```

**`.gitignore`** (replace whatever's in yours with this)
```
node_modules/
```

That's it. So your repo should look like:
```
index.html
server.js
package.json
.gitignore
